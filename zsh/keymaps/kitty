# TITLE titty hot keyboard

kitty_mod ctrl+shift                  #kitty专属超级键

 alt+enter new_window             #新建分割窗口
 ctrl+q close_window              #关闭窗口
 
 super+] next_window              #下一个窗口
 super+[ previous_window          #上一个窗口
 kitty_mod+f move_window_forward  #前移
 kitty_mod+b move_window_backward #后移
 kitty_mod+` move_window_to_top   #上移
 
 kitty_mod+r start_resizing_window #调整窗口大小
 alt+/ focus_visible_window       #快速切换窗口
 kitty_mod+f8 swap_with_window    #视觉上与另一个窗口交换
 ctrl+方向键                       #移动焦点
 shift+方向键                      #移动窗口
 
 ctrl+tab                         #next_tab
 ctrl+shift+tab                   #previous_tab
 ctrl+t new_tab                    #新建tab
 ctrl+shift+q close_tab           #关闭tab
 ctrl+shift+t set_tab_title       #设置标题
 
 # super+alt+/ next_layout          #下一个布局
 
 
